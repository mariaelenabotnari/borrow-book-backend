spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/borrow_book
    driver-classname: org.postgresql.Driver
    username: postgres
    password: maria2004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
